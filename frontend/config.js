// Fill in your Google Maps JS API key (frontend-only, HTTP-referrer restricted).
// Copy this file to config.local.js if you want to keep the key out of git.
window.APP_CONFIG = {
  googleMapsKey: "YOUR_GOOGLE_MAPS_API_KEY",
  backendUrl: "http://localhost:8000",
};
